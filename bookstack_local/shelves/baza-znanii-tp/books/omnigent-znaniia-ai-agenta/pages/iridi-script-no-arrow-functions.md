# no_arrow_functions

**Категория:** iRidi Script
**Источник:** built-in constraint

---

iRidi Script does NOT support arrow/lambda functions, let/const, setTimeout/setInterval, or JSON. Use IR.SetTimeout, IR.SetInterval, IR.Log(). Server tags write via IR.GetServer().Set().